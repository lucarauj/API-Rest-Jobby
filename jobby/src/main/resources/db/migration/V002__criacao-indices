create index idx_cadastros
ON cadastro(id);

create index cadastro_experiencias
ON cadastro_experiencia(id);

create index idx_cidades
ON cidade(id);

create index idx_empresas
ON empresa(id);

create index idx_pretensoes_salariais
ON pretensao_salarial(id);

create index idx_profissoes
ON profissao(id);