create index idx_cadastros
ON cadastro(id);

create index cadastro_experiencias
ON cadastro_experiencia(id);

create index idx_cidades_id
ON cidade(id);

create index idx_empresas_id
ON empresa(id);

create index idx_profissoes_id
ON profissao(id);

